#include<bits/stdc++.h>
using namespace std;

int main(){
    string x;
    string y;
    getline(cin,x);
    cin.ignore(); // to ignore leftover new line as not to print blank string
    getline(cin,y);
    cout << "Gaurav's age is: " << x << endl;
    cout << "Gaurav's legal name is: " << y << endl;
    return 0;
    
}